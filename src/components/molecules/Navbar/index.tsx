import { ReactElement } from 'react';

const Navbar = (): ReactElement => {
    return (
        <nav className="min-h-[7vh] flex items-center px-4">
            <h1 className="text-xl">NextJS Assignment</h1>
        </nav>
    );
};

export default Navbar;
