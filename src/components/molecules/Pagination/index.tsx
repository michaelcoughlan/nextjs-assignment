import { ReactElement } from 'react';

import { Props } from './types';

const Pagination = ({ currentPage, pageSize }: Props): ReactElement => {
    return (
        <div>
            Pagination works!
        </div>
    )
};

export default Pagination;
